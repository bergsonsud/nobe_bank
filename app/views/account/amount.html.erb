<%=render 'composer/transactions/report_options'%>
<h3>Extrato</h3>
<% @dates.each  do |date| %>
  <h5><%=date.strftime("%d/%m/%Y")%></h5>
  <% @transactions.where('date = ?',date).each  do |transaction| %>
    <%= render transaction %>
    <br><br>
  <% end %>
<% end %>
