<div class="card-action">
	<% if transaction.type_transaction == 'deposit' %>
		<i class="material-icons tiny green-text">call_made</i>
	<% else %>
		<i class="material-icons tiny red-text">call_received</i>
	<% end %>

	<%= Transaction.human_enum_name(:type_transaction, transaction.type_transaction) %>

	<%=number_to_currency transaction.transaction_money_moviment%>

	<% if transaction.type_transaction == 'transfer' %>
		<div class="transfer-description">
			<%= String.new().concat('Transferência ', number_to_currency(transaction.value), ' | ', 'Taxa ', number_to_currency(transaction.tax) )%>
		</div>
	<% end %>
</div>
